# Autorunne 0.6.34 State Reliability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (- [ ]) syntax for tracking.

**Goal:** Ship Autorunne 0.6.34 as a backward-compatible reliability patch with serialized state mutation, atomic recoverable JSON persistence, non-destructive memory archives, and consistent version metadata.

**Architecture:** Add a focused persistence module and route existing state-engine persistence through it. Protect complete read-modify-write-render operations with one re-entrant workspace lock, retain a last-known-good JSON backup, repair only safe interrupted-write cases, and make monthly archive batches append-only and idempotent.

**Tech Stack:** Python 3.11+, standard-library OS locking, Typer, Pydantic, pytest, setuptools/build.

---

## Working rules

- Read AGENTS.md, .autorunne/views/START_HERE.md, and the approved design before editing.
- Work on a dedicated branch/worktree based on f1828d4 or its reviewed successor.
- Never edit .autorunne/state/* directly; use Autorunne commands for workflow state.
- Do not commit .venv, .autorunne, dist, caches, or unrelated files.
- Use TDD: failing test, observed failure, minimal implementation, passing test, focused commit.
- Do not change daemon scanning or Git worktree support.
- Do not push, merge, tag, release, publish to PyPI, or post to GitHub Discussions.
- Run tests with AUTORUNNE_DISABLE_UPDATE_CHECK=1.

## File map

Create:

- src/autorunne/core/persistence.py — lock, atomic replacement, recovery, JSONL helpers.
- tests/test_persistence.py — focused persistence tests.
- docs/Autorunne-Release-Notes-0.6.34-ZH.md — truthful candidate notes.
- CODEX_REVIEW_HANDOFF.md — independent-review evidence.

Modify:

- src/autorunne/core/paths.py — delegate JSON/text I/O.
- src/autorunne/core/state_engine.py — lock composite operations and durable event I/O.
- src/autorunne/core/memory.py — safe compaction and archive merging.
- src/autorunne/__init__.py, src/autorunne/models/config.py, pyproject.toml — version contract.
- tests/test_state_engine.py, tests/test_memory_commands.py, tests/test_cli.py, tests/test_docs.py — regressions.
- README.md, CHANGELOG.md and the two tracked repo skill mirrors — release-candidate truth.

## Task 1: Add failing reliability tests

**Files:**

- Create: tests/test_persistence.py
- Modify: tests/test_state_engine.py
- Modify: tests/test_memory_commands.py

- [ ] **Step 1: Write atomic backup/recovery tests**

Create tests/test_persistence.py with imports and these behaviors:

    from __future__ import annotations

    import json
    import multiprocessing
    import time
    from pathlib import Path

    import pytest

    from autorunne.core.persistence import (
        StateCorruptionError,
        StateLockTimeoutError,
        atomic_write_json,
        read_json_recovering,
        workspace_lock,
    )


    def _hold_lock(repo: str, ready: multiprocessing.Event) -> None:
        with workspace_lock(Path(repo), timeout=2.0):
            ready.set()
            time.sleep(0.8)


    def test_atomic_json_keeps_last_known_good_backup(tmp_path: Path):
        target = tmp_path / ".autorunne" / "state" / "sessions.json"
        atomic_write_json(target, {"items": [{"title": "first"}]})
        atomic_write_json(target, {"items": [{"title": "second"}]})
        assert json.loads(target.read_text())["items"][0]["title"] == "second"
        backup = target.with_name("sessions.json.bak")
        assert json.loads(backup.read_text())["items"][0]["title"] == "first"


    def test_malformed_primary_recovers_from_valid_backup(tmp_path: Path):
        target = tmp_path / ".autorunne" / "state" / "sessions.json"
        atomic_write_json(target, {"items": [{"title": "first"}]})
        atomic_write_json(target, {"items": [{"title": "second"}]})
        target.write_text("{", encoding="utf-8")
        recovered = read_json_recovering(target, default={})
        assert recovered == {"items": [{"title": "first"}]}
        assert json.loads(target.read_text()) == recovered


    def test_malformed_primary_and_backup_fail_closed(tmp_path: Path):
        target = tmp_path / ".autorunne" / "state" / "sessions.json"
        target.parent.mkdir(parents=True)
        target.write_text("{", encoding="utf-8")
        target.with_name("sessions.json.bak").write_text("[", encoding="utf-8")
        with pytest.raises(StateCorruptionError, match="sessions.json"):
            read_json_recovering(target, default={})
        assert target.read_text() == "{"
        assert target.with_name("sessions.json.bak").read_text() == "["


    def test_workspace_lock_times_out_while_other_process_holds_it(tmp_path: Path):
        repo = tmp_path / "repo"
        repo.mkdir()
        ready = multiprocessing.Event()
        process = multiprocessing.Process(target=_hold_lock, args=(str(repo), ready))
        process.start()
        assert ready.wait(timeout=2.0)
        try:
            with pytest.raises(StateLockTimeoutError, match="retry"):
                with workspace_lock(repo, timeout=0.1, poll_interval=0.01):
                    pass
        finally:
            process.join(timeout=2.0)
            if process.is_alive():
                process.terminate()

Also import atomic_write_jsonl and read_jsonl_recovering and add:

    def test_jsonl_repairs_only_an_incomplete_final_line(tmp_path: Path):
        target = tmp_path / ".autorunne/state/events.jsonl"
        target.parent.mkdir(parents=True)
        target.write_text(
            '{"type":"first"}\n{"type":"second"', encoding="utf-8"
        )
        assert read_jsonl_recovering(target) == [{"type": "first"}]
        assert target.read_text(encoding="utf-8") == '{"type": "first"}\n'


    def test_jsonl_middle_corruption_fails_closed(tmp_path: Path):
        target = tmp_path / ".autorunne/state/events.jsonl"
        target.parent.mkdir(parents=True)
        original = '{"type":"first"}\n{bad}\n{"type":"third"}\n'
        target.write_text(original, encoding="utf-8")
        with pytest.raises(StateCorruptionError, match="before the final line"):
            read_jsonl_recovering(target)
        assert target.read_text(encoding="utf-8") == original


    def test_workspace_lock_is_reentrant_in_one_thread(tmp_path: Path):
        repo = tmp_path / "repo"
        repo.mkdir()
        with workspace_lock(repo, timeout=0.1):
            with workspace_lock(repo, timeout=0.1):
                atomic_write_json(
                    repo / ".autorunne/state/current.json", {"ok": True}
                )


    def test_failed_primary_replace_leaves_previous_json_complete(
        tmp_path: Path, monkeypatch
    ):
        import autorunne.core.persistence as persistence

        target = tmp_path / ".autorunne/state/current.json"
        atomic_write_json(target, {"generation": "old"})
        original_replace = persistence.os.replace

        def fail_primary_replace(source, destination):
            if Path(destination) == target:
                raise OSError("simulated interrupted replace")
            return original_replace(source, destination)

        monkeypatch.setattr(persistence.os, "replace", fail_primary_replace)
        with pytest.raises(OSError, match="interrupted replace"):
            atomic_write_json(target, {"generation": "new"})
        assert json.loads(target.read_text()) == {"generation": "old"}
        assert list(target.parent.glob(f".{target.name}.*.tmp")) == []

- [ ] **Step 2: Add real concurrent CLI coverage**

Add concurrent.futures, subprocess, and sys imports to tests/test_state_engine.py. Add a test that:

1. adopts python_repo;
2. starts forty subprocesses running the real task-add CLI with unique texts;
3. uses at most sixteen worker threads;
4. asserts every return code is zero;
5. reloads tasks.json and asserts all forty texts exist.

Use the actual option order returned by:

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/autorunne task add --help

The assertion must remain:

    expected = {f"concurrent task {index}" for index in range(40)}
    tasks = json.loads((python_repo / ".autorunne/state/tasks.json").read_text())
    assert expected <= {item["text"] for item in tasks["next_up"]}

- [ ] **Step 3: Add same-month archive and retry tests**

In tests/test_memory_commands.py, perform one compaction of six January records, add six distinct January records, compact again, then assert both old markers remain and exactly two archive-batch markers exist.

Add a second test that monkeypatches memory.save_workspace_state to fail once after archive persistence, reruns the same compaction, and asserts:

    assert archive.count("<!-- autorunne-archive-batch:") == 1
    assert archive.count("test session 0") == 1

- [ ] **Step 4: Observe RED**

Run:

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest \
      tests/test_persistence.py \
      tests/test_state_engine.py::test_concurrent_task_add_preserves_every_unique_task \
      tests/test_memory_commands.py::test_two_same_month_compactions_preserve_both_batches \
      tests/test_memory_commands.py::test_archive_batch_write_is_idempotent -q

Expected: failure because the persistence module is absent; after imports exist, concurrency/archive tests still fail against 0.6.33.

- [ ] **Step 5: Commit red tests**

    git add tests/test_persistence.py tests/test_state_engine.py tests/test_memory_commands.py
    git commit -m "test: define 0.6.34 state reliability contract"

## Task 2: Implement persistence primitives

**Files:**

- Create: src/autorunne/core/persistence.py
- Test: tests/test_persistence.py

- [ ] **Step 1: Implement errors and re-entrant lock**

The module must expose:

    class StateCorruptionError(RuntimeError):
        pass


    class StateLockTimeoutError(RuntimeError):
        pass


    @contextmanager
    def workspace_lock(
        repo_root: Path,
        *,
        timeout: float = 10.0,
        poll_interval: float = 0.05,
    ) -> Iterator[None]:
        state = _lock_state(repo_root)
        state.gate.acquire()
        try:
            if state.depth == 0:
                path = repo_root / ".autorunne/runtime/state.lock"
                path.parent.mkdir(parents=True, exist_ok=True)
                handle = path.open("a+b")
                deadline = time.monotonic() + timeout
                while not _try_os_lock(handle):
                    if time.monotonic() >= deadline:
                        handle.close()
                        raise StateLockTimeoutError(
                            f"Autorunne state is busy for {repo_root}. "
                            "Wait for the other command to finish, then retry."
                        )
                    time.sleep(poll_interval)
                state.handle = handle
            state.depth += 1
            try:
                yield
            finally:
                state.depth -= 1
                if state.depth == 0 and state.handle is not None:
                    _unlock_os(state.handle)
                    state.handle.close()
                    state.handle = None
        finally:
            state.gate.release()

    def workspace_locked(function: F) -> F:
        @wraps(function)
        def wrapped(repo_root: Path, *args: Any, **kwargs: Any):
            with workspace_lock(repo_root):
                return function(repo_root, *args, **kwargs)
        return wrapped

Use a registry keyed by repo_root.resolve(). Each entry owns a threading.RLock, nesting depth, and OS-lock handle. The outermost entry opens .autorunne/runtime/state.lock, polls a nonblocking OS lock until timeout, and the outermost exit unlocks/closes it. Use fcntl.flock on POSIX and msvcrt.locking on Windows. File existence is not lock ownership.

The timeout exception text must contain the repo path and: "Wait for the other command to finish, then retry."

- [ ] **Step 2: Implement atomic replacement exactly once**

Implement:

    def _atomic_replace_bytes(path: Path, content: bytes) -> None:
        path.parent.mkdir(parents=True, exist_ok=True)
        descriptor, temp_name = tempfile.mkstemp(
            prefix=f".{path.name}.",
            suffix=".tmp",
            dir=path.parent,
        )
        temp_path = Path(temp_name)
        try:
            with os.fdopen(descriptor, "wb") as handle:
                handle.write(content)
                handle.flush()
                os.fsync(handle.fileno())
            os.replace(temp_path, path)
            _fsync_directory(path.parent)
        finally:
            if temp_path.exists():
                temp_path.unlink()

_fsync_directory must no-op on Windows and open/fsync/close the directory on POSIX.

- [ ] **Step 3: Implement atomic JSON and recovery**

Required signatures:

    def atomic_write_text(path: Path, content: str) -> None:
        _atomic_replace_bytes(path, content.encode("utf-8"))


    def atomic_write_json(path: Path, payload: Any) -> None:
        serialized = json.dumps(
            payload, indent=2, ensure_ascii=False
        ) + "\n"
        # If current primary parses, atomically copy its exact bytes to NAME.bak.
        # Never replace a good backup with malformed primary bytes.
        # Atomically replace primary with serialized.


    def read_json_recovering(path: Path, default: Any | None = None) -> Any:
        # Missing primary returns the existing default contract.
        # Valid primary returns parsed JSON.
        # Invalid primary + valid NAME.bak restores backup atomically and returns it.
        # Invalid primary + invalid/missing backup raises StateCorruptionError.

The fail-closed error must say that Autorunne did not reset state and identify both primary and backup paths.

- [ ] **Step 4: Implement JSONL helpers**

Expose:

    def atomic_write_jsonl(path: Path, items: list[dict[str, Any]]) -> None:
        content = "".join(
            json.dumps(item, ensure_ascii=False) + "\n" for item in items
        )
        backup = path.with_name(path.name + ".bak")
        if path.exists():
            current = path.read_text(encoding="utf-8")
            _parse_jsonl(current, path, allow_tail_repair=False)
            _atomic_replace_bytes(backup, current.encode("utf-8"))
        atomic_write_text(path, content)

    def read_jsonl_recovering(path: Path) -> list[dict[str, Any]]:
        if not path.exists():
            atomic_write_text(path, "")
            return []
        text = path.read_text(encoding="utf-8")
        events, repaired_tail = _parse_jsonl(
            text, path, allow_tail_repair=True
        )
        if repaired_tail:
            content = "".join(
                json.dumps(item, ensure_ascii=False) + "\n"
                for item in events
            )
            atomic_write_text(path, content)
        return events

One malformed final non-empty line is an interrupted append: retain every earlier valid event and atomically rewrite the valid list. A malformed middle line raises StateCorruptionError and does not rewrite the file. Preserve a valid previous events.jsonl as events.jsonl.bak before a compaction rewrite.

- [ ] **Step 5: Run GREEN and commit**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest tests/test_persistence.py -q
    git add src/autorunne/core/persistence.py tests/test_persistence.py
    git commit -m "feat: add recoverable atomic state persistence"

Expected: focused persistence tests pass.

## Task 3: Integrate persistence with state operations

**Files:**

- Modify: src/autorunne/core/paths.py
- Modify: src/autorunne/core/state_engine.py
- Modify: tests/test_state_engine.py

- [ ] **Step 1: Delegate path I/O**

Import atomic_write_json, atomic_write_text, and read_json_recovering in paths.py. Use:

    def read_json(path: Path, default: Any | None = None) -> Any:
        return read_json_recovering(path, default=default)


    def write_json(path: Path, payload: Any) -> None:
        atomic_write_json(path, payload)


    def write_text(path: Path, content: str) -> None:
        atomic_write_text(path, content.rstrip() + "\n")

Change migrate_config and save_config to call write_json rather than Path.write_text.

- [ ] **Step 2: Make event append durable**

In state_engine.py, append_event must be decorated with workspace_locked. Serialize before opening, append one line, flush, and os.fsync the descriptor. load_events must be workspace_locked and delegate to read_jsonl_recovering. _ensure_event_file must use the atomic write_text helper.

- [ ] **Step 3: Lock complete transactions**

Add workspace_locked to these functions:

- load_workspace_state
- save_workspace_state
- render_views
- repair_handoff_state
- bootstrap_workspace
- sync_workspace
- start_task
- record_checkpoint
- finish_task
- record_task_ingress
- record_hermes_ingress
- record_integration
- manual_record
- migrate_workspace
- mutate_task_list

Do not lock scanners, validations, builds, or update checks. Nested calls are intentionally re-entrant.

- [ ] **Step 4: Add CLI recovery tests**

Add parametrized tests that create a backup through two valid writes, corrupt sessions.json, and separately run open and doctor --handoff. Each command must exit zero and leave a parseable restored primary.

Add parametrized tests that corrupt both sessions.json and sessions.json.bak, then separately run open and doctor --handoff. Each command must exit 1, print "Autorunne state is corrupt" and "did not reset state", and preserve both corrupt inputs byte-for-byte.

- [ ] **Step 5: Run state tests and commit**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest \
      tests/test_persistence.py tests/test_state_engine.py -q
    git add src/autorunne/core/paths.py src/autorunne/core/state_engine.py tests/test_state_engine.py
    git commit -m "fix: serialize and recover Autorunne state updates"

Expected: all pass, including forty concurrent unique task additions.

## Task 4: Make compaction append-only and idempotent

**Files:**

- Modify: src/autorunne/core/memory.py
- Modify: tests/test_memory_commands.py

- [ ] **Step 1: Add a deterministic batch ID**

Use canonical JSON and SHA-256:

    def _archive_batch_id(
        sessions: list[dict[str, Any]],
        events: list[dict[str, Any]],
    ) -> str:
        canonical = json.dumps(
            {"sessions": sessions, "events": events},
            ensure_ascii=False,
            sort_keys=True,
            separators=(",", ":"),
        )
        return hashlib.sha256(canonical.encode("utf-8")).hexdigest()

- [ ] **Step 2: Merge one archive batch atomically**

Implement _merge_archive_batch with these invariants:

    marker = f"<!-- autorunne-archive-batch:{batch_id} -->"
    existing = (
        archive_path.read_text(encoding="utf-8")
        if archive_path.exists()
        else f"# Autorunne Archive - {month}\n"
    )
    if marker in existing:
        return False

The new section heading is "## Compaction batch — TIMESTAMP". Use "### Session summary" and "### Event summary", existing bullet helpers, and atomic_write_text on existing.rstrip() + two newlines + new section. Return True when written.

- [ ] **Step 3: Protect and atomically persist compaction**

Decorate compact_memory with workspace_locked. Compute one timestamp before archive writes. Replace _write_events with atomic_write_jsonl. Replace direct SUMMARY.md.write_text with atomic_write_text. Never replace an existing monthly archive.

- [ ] **Step 4: Run memory tests and commit**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest tests/test_memory_commands.py -q
    git add src/autorunne/core/memory.py tests/test_memory_commands.py
    git commit -m "fix: preserve idempotent monthly memory archives"

Expected: existing compaction tests and both new archive tests pass.

## Task 5: Align version metadata and isolate update tests

**Files:**

- Modify: src/autorunne/__init__.py
- Modify: src/autorunne/models/config.py
- Modify: pyproject.toml
- Modify: tests/test_cli.py
- Modify: .agents/skills/autorunne-workflow/SKILL.md
- Modify: .claude/skills/autorunne-workflow/SKILL.md

- [ ] **Step 1: Add the version contract test**

Add import tomllib and:

    def test_release_version_contract_matches_runtime_config_and_metadata():
        from autorunne.models.config import WorkflowConfig

        root = Path(__file__).resolve().parents[1]
        metadata = tomllib.loads(
            (root / "pyproject.toml").read_text(encoding="utf-8")
        )
        assert __version__ == "0.6.34"
        assert metadata["project"]["version"] == __version__
        assert WorkflowConfig().version == __version__

- [ ] **Step 2: Isolate the 9.9.9 update test**

Give test_update_check_command_prints_reminder_without_upgrading a tmp_path fixture and invoke:

    runner.invoke(
        app,
        [
            "update-check",
            "--latest-version",
            "9.9.9",
            "--path",
            str(tmp_path),
        ],
        catch_exceptions=False,
    )

Assert the cache exists below tmp_path/.autorunne/runtime and never inspect or write the source checkout cache.

- [ ] **Step 3: Apply version values**

- src/autorunne/__init__.py: __version__ = "0.6.34"
- pyproject.toml: version = "0.6.34"
- models/config.py: import __version__ as AUTORUNNE_VERSION and use version: str = AUTORUNNE_VERSION.
- Change only the version front matter in both repo skills to 0.6.34. Preserve their intentional Codex-versus-Claude source/wrapper wording.

- [ ] **Step 4: Verify and commit**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest \
      tests/test_cli.py::test_version_command_and_option_print_package_version \
      tests/test_cli.py::test_update_check_command_prints_reminder_without_upgrading \
      tests/test_cli.py::test_release_version_contract_matches_runtime_config_and_metadata \
      tests/test_update_check.py tests/test_integrations.py -q
    test "$(sed -n 's/^version: //p' .agents/skills/autorunne-workflow/SKILL.md)" = \
      "$(sed -n 's/^version: //p' .claude/skills/autorunne-workflow/SKILL.md)"
    git add pyproject.toml src/autorunne/__init__.py \
      src/autorunne/models/config.py tests/test_cli.py \
      .agents/skills/autorunne-workflow/SKILL.md \
      .claude/skills/autorunne-workflow/SKILL.md
    git commit -m "chore: align Autorunne 0.6.34 version metadata"

Expected: tests pass and both repo skills report the same 0.6.34 version while retaining agent-specific wording.

## Task 6: Full verification and package build

**Files:**

- Modify only already-listed files if a test proves an in-scope defect.

- [ ] **Step 1: Run focused suites**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest \
      tests/test_persistence.py tests/test_state_engine.py \
      tests/test_memory_commands.py tests/test_update_check.py -q

Expected: all pass.

- [ ] **Step 2: Run the full suite**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest -q

Expected: all pass.

- [ ] **Step 3: Build and inspect metadata**

    .venv/bin/python -m build
    unzip -p dist/autorunne-0.6.34-py3-none-any.whl \
      'autorunne-0.6.34.dist-info/METADATA' | sed -n '1,20p'

Expected: wheel and sdist build; metadata contains Name: autorunne and Version: 0.6.34.

- [ ] **Step 4: Check scope and hygiene**

    git diff --check
    git status --short
    git diff --name-only f1828d4 HEAD

Expected: no .venv, .autorunne, dist, caches, daemon implementation, worktree implementation, or unrelated files in commits.

- [ ] **Step 5: Handle a test-proven correction at its owning task**

If a focused/full test exposes an in-scope defect, return to the task that owns that file, add a regression test there, rerun that task's focused command, and amend neither older commits nor unrelated files. Create a new focused commit named "fix: close 0.6.34 reliability regression". If no correction is needed, create no commit.

## Task 7: Prepare candidate docs and Codex handoff

**Files:**

- Create: docs/Autorunne-Release-Notes-0.6.34-ZH.md
- Modify: README.md
- Modify: CHANGELOG.md
- Modify: tests/test_docs.py
- Create: CODEX_REVIEW_HANDOFF.md

- [ ] **Step 1: Write truthful Chinese release notes**

The notes must say that 0.6.34 preserves the current UX while adding concurrent-write serialization, atomic JSON plus last-good recovery, fail-closed unrecoverable corruption, same-month append-only compaction, retry idempotency, and version/test hygiene.

Include an explicit excluded section for daemon, Git worktrees, database/cloud migration, and publication.

State "release candidate" until GitHub tag, GitHub Release, and PyPI have actually succeeded.

- [ ] **Step 2: Update README and changelog narrowly**

Add a 0.6.34 - 2026-08-22 changelog section with Fixed, Reliability, and Verification subsections based only on observed results.

Replace stale README current-version copy with a 0.6.34 release-candidate section and link the 0.6.34 notes. Do not claim GitHub/PyPI publication.

Add RELEASE_NOTES_0634 to tests/test_docs.py and a focused test asserting the notes contain "0.6.34", "原子", "备份", "同月", "release candidate", and the explicit daemon/worktree exclusions. The test must not assert that GitHub or PyPI publication succeeded.

- [ ] **Step 3: Create CODEX_REVIEW_HANDOFF.md**

Use these required sections:

- Status: one of COMPLETE, PARTIAL, BLOCKED.
- Workspace: absolute cwd, branch, base SHA, head SHA.
- Scope implemented and explicit exclusions.
- Changed files with one purpose each.
- Requirement matrix covering concurrency, atomic backup, recovery, fail-closed behavior, archive preservation, retry idempotency, and 0.6.34 metadata.
- Exact commands, exit codes, concise output.
- Known limitations.
- Publication fields explicitly saying NOT PERFORMED.
- A request for Codex to independently inspect and rerun focused tests, full suite, build, package metadata, and scope diff.

No claimed PASS may lack a real command result.

- [ ] **Step 4: Final verification**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest tests/test_docs.py -q
    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest -q
    .venv/bin/python -m build
    git diff --check

Expected: every command exits zero.

- [ ] **Step 5: Record workflow completion**

    AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/autorunne finish \
      --summary "Implemented and verified the bounded Autorunne 0.6.34 state reliability patch" \
      --next "Codex independently reviews CODEX_REVIEW_HANDOFF.md and reruns reliability verification" \
      --validate "AUTORUNNE_DISABLE_UPDATE_CHECK=1 .venv/bin/python -m pytest -q"

Do not edit .autorunne/state directly and do not add .autorunne to Git.

- [ ] **Step 6: Commit candidate documentation**

    git add README.md CHANGELOG.md \
      docs/Autorunne-Release-Notes-0.6.34-ZH.md \
      CODEX_REVIEW_HANDOFF.md
    git commit -m "docs: prepare Autorunne 0.6.34 review handoff"

## Final stop condition

Stop after the handoff commit. Report branch, head SHA, and COMPLETE/PARTIAL/BLOCKED. Do not merge main, push, tag, publish, or change an external system. Independent Codex review is the next gate.
